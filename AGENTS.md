# Acme consumer demo

Run the checks in `.github/workflows/ci.yml` after changing any language implementation. The
dependency roster below is generated; use `git-a2a sync` instead of editing its managed block.

<!-- git-a2a:begin -->
## git-a2a module roster

### This module: `consumer-app`

Polyglot consumer of the Acme utility demo module.

- `acme-app-cli` (owner), scope: `**`
- may: `read-surface`, `ask`, `open-issue`, `propose-change`
- may-not: `commit`, `release`
- notes: Changes to the consumer are proposed through its declared owner contact.

### Dependency: `acme-lib-utils`

> Description (from acme-lib-utils @dcaf2b1e6ff0): Shared formatting utilities with the same contract in TypeScript, Python, and Go.
>
> Published surface (from acme-lib-utils @dcaf2b1e6ff0): `.git-a2a/cache/acme-lib-utils/surface/` (available after `git-a2a show acme-lib-utils --surface`).
>
> Consumer policy (from acme-lib-utils @dcaf2b1e6ff0):
> - may: `read-surface`, `ask`, `open-issue`, `propose-change`
> - may-not: `commit`, `edit-spec`, `release`
> - notes: Consumers propose changes through the declared contacts; the library owner changes code.

| Intent | Agent (role) | Declared contact |
| --- | --- | --- |
| question | acme-lib-utils (owner) | A2A https://git-a2a.com/demo/agents/acme-lib-utils/; note: The public demo card is static; use the issue contact for a delivered request. |
| question | acme-lib-utils (owner) | mattermost channel acme-demo handle @acme-lib-utils; note: This chat contact demonstrates instruction-only routing. |
| change | acme-pm (spec) | github-issue neprel/git-a2a-demo-acme-lib labels=change-request,from-agent; note: Describe the consumer, the need, and the affected languages. |
| bug | acme-lib-utils (owner) | github-issue neprel/git-a2a-demo-acme-lib labels=from-agent |
| review | acme-lib-utils (owner) | github-issue neprel/git-a2a-demo-acme-lib labels=from-agent |
| incident | — | none declared |

Generated by git-a2a from `a2amodule.yml` and `a2amodule.lock`; edits inside this block are overwritten. Use `git-a2a who <id> --intent <intent>` for the full contact list.
Run `git-a2a usage` to learn the tool.
<!-- git-a2a:end -->

## git-a2a skill

Read `skills/git-a2a/SKILL.md` for dependency and ownership recipes; run `git-a2a usage` for the
compact briefing.
