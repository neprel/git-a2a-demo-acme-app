---
name: git-a2a
description: Use for git module dependencies, a2amodule.yml, dependency owners, contacts, status, and the managed AGENTS.md roster.
compatibility: requires git >= 2.25 and the git-a2a CLI
metadata:
  version: 1.0.1
---

# git-a2a

Run `git-a2a usage` for the compact workflow and `git-a2a usage --prompt` before unfamiliar
authoring or dependency work. Use `git-a2a explain <path>` for one normative manifest field.
Read [references/README.md](references/README.md) for the canonical full-reference locations.
