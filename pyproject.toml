[project]
name = "consumer-app"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "acme-lib-utils",
]

[tool.uv]
package = false

[tool.uv.sources]
"acme-lib-utils" = { path = "deps/acme-lib-utils" }
